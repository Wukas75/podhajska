{
  "name": "podhajska-web",
  "version": "1.0.0",
  "private": true,
  "description": "Statická landing page Štúdiá Podhájska + admin (Cloudflare Pages + Functions + D1 + R2)",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-node.mjs",
    "dev:reset": "node scripts/dev-node.mjs --reset",
    "dev:cf": "wrangler pages dev public --d1=DB --r2=R2 --compatibility-date=2026-01-01 --port 8788",
    "db:remote": "wrangler d1 execute podhajska --remote --file migrations/0001_init.sql && wrangler d1 execute podhajska --remote --file migrations/0002_seed.sql",
    "assets": "bash scripts/fetch-assets.sh",
    "deploy": "wrangler pages deploy public --project-name=podhajska",
    "hash": "node scripts/hash-password.mjs"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
